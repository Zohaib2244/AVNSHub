services:
  avn-hub:
    build: .
    container_name: avn-hub
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local          # real secrets — never baked into the image
